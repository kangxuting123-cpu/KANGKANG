const app=document.querySelector('[data-room-app]');let duration=60;let selected='春分';
app.querySelectorAll('[data-people]').forEach(button=>button.addEventListener('click',()=>{app.querySelectorAll('[data-people]').forEach(item=>item.classList.toggle('active',item===button));const need=Number(button.dataset.people);app.querySelectorAll('[data-room]').forEach(room=>{room.hidden=Number(room.dataset.size)<need});const visible=[...app.querySelectorAll('[data-room]')].find(room=>!room.hidden);if(visible)visible.click()}));
app.querySelectorAll('[data-time]').forEach(button=>button.addEventListener('click',()=>{app.querySelectorAll('[data-time]').forEach(item=>item.classList.toggle('active',item===button));duration=Number(button.dataset.time);updateSlot()}));
function updateSlot(){const end=new Date(2026,8,1,14,35+duration);app.querySelector('[data-slot]').textContent=`14:35—${String(end.getHours()).padStart(2,'0')}:${String(end.getMinutes()).padStart(2,'0')}`}
app.querySelectorAll('[data-room]').forEach(room=>room.addEventListener('click',()=>{app.querySelectorAll('[data-room]').forEach(item=>item.classList.toggle('active',item===room));selected=room.dataset.room;app.querySelector('[data-name]').textContent=selected;app.querySelector('[data-capacity]').textContent=`${room.dataset.size} 人`;app.querySelector('[data-book]').textContent='立即预订';app.querySelector('[data-book]').disabled=false}));
app.querySelector('[data-book]').addEventListener('click',event=>{const title=app.querySelector('[data-title]').value.trim()||'临时会议';event.currentTarget.textContent='✓ 已预订';event.currentTarget.disabled=true;const toast=app.querySelector('[data-toast]');toast.textContent=`${selected} · ${title} 已加入飞书日历`;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2300)});
app.querySelector('[data-floor]').addEventListener('change',event=>{app.querySelector('.map-title>span').textContent=`${event.target.value}F · 西区`;const toast=app.querySelector('[data-toast]');toast.textContent=`已切换到 ${event.target.value}F 实时地图`;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1500)});
